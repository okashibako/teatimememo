head(){
    const that = this;
      var promise = that.second();
        promise.then(function (result) {
        const i = 1 + result;
        console.log(i);
      })
  }
  second(){
        return new Promise((resolve, reject) => {
            const j = 1;
            console.log(j);
            resolve(j)
        });
  }